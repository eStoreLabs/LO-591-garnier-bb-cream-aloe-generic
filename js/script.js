  var swiper = new Swiper('#es-rc #es-garnier .swiper', {
    // Optional parameters
    // direction: 'horizontal',
    loop: true,
    autoHeight: false,

    // Navigation arrows
    navigation: {
      nextEl: '#es-rc #es-garnier .swiper-button-next',
      prevEl: '#es-rc #es-garnier .swiper-button-prev'
    },
    pagination: {
      el: '#es-rc #es-garnier .swiper-pagination',
      clickable: true
    }
  });
  var swiper = new Swiper('#es-rc #es-garnier .swiper-cross', {
    // Optional parameters
    // direction: 'horizontal',
    loop: true,
    autoHeight: false,

    // Navigation arrows
    navigation: {
      nextEl: '#es-rc #es-garnier .swiper-cross-button-next',
      prevEl: '#es-rc #es-garnier .swiper-cross-button-prev'
    },
    pagination: {
      el: '#es-rc #es-garnier .swiper-cross-pagination',
      clickable: true
    }
  });